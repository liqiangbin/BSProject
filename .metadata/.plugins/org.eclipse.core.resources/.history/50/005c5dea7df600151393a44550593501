package com.cn.hnust.controller;

import java.text.ParseException;
import java.util.LinkedHashMap;
import java.util.Map;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpSession;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;

import com.cn.hnust.pojo.Manager;
import com.cn.hnust.service.ManagerService;

@Controller  

@RequestMapping("/login")  
public class LoginController {
	@Resource  
    private ManagerService managerService;
	private Map<String,String> whereCondition;
	
	public Map<String, String> getWhereCondition() {
		return whereCondition;
	}

	public void setWhereCondition(Map<String, String> whereCondition) {
		this.whereCondition = whereCondition;
	}

	@RequestMapping("/turnIndex")  
	 public String toIndex(HttpServletRequest request,Model model,Manager manager,HttpSession session) throws ParseException{  
		int error=0;
		//Manager managers=managerService.selectByLogin(manager.getLoginname(), manager.getPassword());
		 whereCondition=new LinkedHashMap<String, String>();
		whereCondition.
		List<Manager> managers= managerService.getManager(manager);
//		if(managers==null){
//        	 error=1;
//        	 model.addAttribute("error", error);
//        	return "login"; 
//        }
		System.out.println("jieguo-->"+managers.size());
        session.setAttribute("manager", managers);
        session.setAttribute("age", "12");
        return "manager/index"; 
    }  

}
